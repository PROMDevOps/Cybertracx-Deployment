apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: nopc-postgres-secret
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: k8s-secret-store      # name of the SecretStore (or kind specified)
  target:
    name: nopc-postgres-secret       # name of the k8s Secret to be created
  data:
  - secretKey: adminPasswordKey
    remoteRef:
      key: initial-secret
      property: PG_NOPCOMMERCE_PASSWORD
